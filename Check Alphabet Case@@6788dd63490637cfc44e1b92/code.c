#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char a, A, Z;
    scanf("%c", &a);
    if(A >=0 && 0<= Z){
    printf("Uppercase");
    }
    else if(a>=0 || z<=0){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}